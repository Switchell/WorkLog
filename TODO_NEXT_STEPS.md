# Что можно сделать позже

Стабильная версия сейчас работает. Стратегия и приоритеты по фазам: **`docs/СОСТОЯНИЕ_И_ROADMAP.md`**.

Ниже — короткий список отложенных улучшений:

- Добавить `@st.fragment` для блока восстановления из бэкапа (чтобы не перерисовывать весь экран).
- Добавить мок-тесты для `run_data_import` и связанных сценариев импорта.
- Зафиксировать (pin) версию образа `certbot` в Docker Compose.
- Перед деплоем проверять, что в образ попадает весь `src/` (включая вспомогательные модули), чтобы избежать `ImportError`.

